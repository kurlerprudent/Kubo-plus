const config = {
  plugins: ["@tailwindcss/postcss"],
  
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', 'sans-serif'],
        heading: ['Manrope', 'sans-serif'],
      },
    },
  }
};

export default config;
